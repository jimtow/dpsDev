
Netlify/GitHub Team name: 500Dev

Push file changes to Github
 or Clone local folder changes to GitHub.

Clone from GitHub

Clone the repository: Open local folder then 
git clone https://github.com/jimtow/dpsDev.git


Work in VS Code GitHub Explorer whenever online??

Simulates two different users on different computers.

Commit and Save changes when finished.

This folder was created by cloning from GitHub as follows:

how do i copy a github folder to my desktop

If by "GitHub folder" you mean a repository that already exists on GitHub, the best way is to clone it directly to your Desktop.

Option 1: Clone the repository to your Desktop (recommended)
Open VS Code or a terminal.
Change to your Desktop folder.

Windows Command Prompt:

cd %USERPROFILE%\Desktop

PowerShell:

cd ~/Desktop
Clone the repository:
git clone https://github.com/USERNAME/REPOSITORY.git

For example:

git clone https://github.com/jimtow/dpsDev.git

Git will create a new folder on your Desktop named after the repository.